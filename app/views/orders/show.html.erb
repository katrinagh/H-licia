<%= render partial: 'order' if @render_order %>

  <!-- Mapping à décaller sur la show -->

  <div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers_user.to_json %>"
  data-map-markers-value="<%= @markers_order.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
  </div>
